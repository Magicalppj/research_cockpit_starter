id: option_flan_t5_clap
type: option
title: FLAN-T5-XL token branch + CLAP anchor
status: active
priority: high
parent: problem_event_text_weak
summary: Main candidate for event/edit branch text encoding.
hypothesis: >
  FLAN-T5 improves token-level event understanding, while CLAP improves
  audio-semantic alignment for old/new edits.
decision_state: promising
evidence_strength: weak
pros:
  - Strong event-level token features.
  - Audio-aligned CLAP semantic anchor.
  - Keeps LTX/Gemma global prompt path unchanged.
cons:
  - Requires new timeline feature cache.
  - Adds training/inference complexity.
implementation_steps:
  - Generate FLAN-T5-XL event and edit token features.
  - Generate CLAP text embeddings for event/old/new texts.
  - Inject CLAP anchor into EventStateInitializer core seed.
expected_outcome: Better replace-following and remove semantic suppression.
failure_modes:
  - CLAP anchor too weak to affect output.
  - Feature cache mismatch between training and inference.
children:
  - exp_041_flan_t5_only
  - exp_042_flan_t5_clap
  - decision_flan_t5_clap
focus_priority: 95
show_in_focus: auto
agent_context:
  include: true
  role: implementation
  key_files:
    - ltx_trainer/text_features/t5_clap_encoder.py
    - ltx_trainer/modules/semantic_ribbon_vnext.py
  next_action_hint: Add CLAP projection and gated anchor injection.
